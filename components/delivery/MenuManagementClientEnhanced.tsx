'use client';

import { MenuManagementClient } from '@/components/delivery/MenuManagementClient';

/**
 * Compatibility wrapper kept for docs/history references.
 * Current production menu UX is implemented in MenuManagementClient.
 */
export function MenuManagementClientEnhanced() {
  return <MenuManagementClient />;
}

